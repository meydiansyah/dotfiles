"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const utils_1 = require("../formatting/prettier/utils");
suite('If Statements', () => {
    test('it can format if statements without any HTML hints', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`@if($true)

@elseif($anotherValue)
@else

@endif


@if($true)
Thing
@elseif($anotherValue)
More Things
@else
Another Thing
@endif`).trim(), `@if($true)
@elseif($anotherValue)
@else
@endif

@if($true)
    Thing
@elseif($anotherValue)
    More Things
@else
    Another Thing
@endif`);
    });
    test('it can format if statements with embedded HTML', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`@if($true)
<span>Hello</span>
@elseif($anotherValue)
<div>
    <p>Hello world</p>
    <div>
    @pair
        Test

@endpair
    </div>
    </div>
@else

<p>Test {{ $title}}     test
        </p>

@endif
`).trim(), `@if($true)
    <span>Hello</span>
@elseif($anotherValue)
    <div>
        <p>Hello world</p>
        <div>
            @pair
                Test
            @endpair
        </div>
    </div>
@else
    <p>Test {{ $title }} test</p>
@endif`);
    });
    test('it can parse and format conditions with lots of whitespace', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`@if         ($true)
            <span>Hello</span>
            @elseif                     ($anotherValue)
            <div>
                <p>Hello world</p>
                <div>
                @pair
                    Test
            
            @endpair
                </div>
                </div>
            @else
            
            <p>Test {{ $title}}     test
                    </p>
            
            @endif`).trim(), `@if($true)
    <span>Hello</span>
@elseif($anotherValue)
    <div>
        <p>Hello world</p>
        <div>
            @pair
                Test
            @endpair
        </div>
    </div>
@else
    <p>Test {{ $title }} test</p>
@endif`);
    });
    test('simple if statement does not add extra indentation', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`@if($true)
            <p>Hello
            </p>
            @endif`).trim(), `@if($true)
    <p>Hello</p>
@endif`);
    });
    test('it can format nested if statements', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`@if($true)
            Thing
        @elseif($anotherValue)
            More Things
        @else
            @if($true)
            Thing
        @elseif($anotherValue)
            More Things
        @else
            Another Thing
        @endif
        @endif`).trim(), `@if($true)
    Thing
@elseif($anotherValue)
    More Things
@else
    @if($true)
        Thing
    @elseif($anotherValue)
        More Things
    @else
        Another Thing
    @endif
@endif`);
    });
    test('it can format without an else', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`@if($true)
            Thing
                        @elseif($anotherValue) More Things @endif`).trim(), `@if($true)
    Thing
@elseif($anotherValue)
    More Things
@endif`);
    });
});
//# sourceMappingURL=formatter_if_statements.test.js.map