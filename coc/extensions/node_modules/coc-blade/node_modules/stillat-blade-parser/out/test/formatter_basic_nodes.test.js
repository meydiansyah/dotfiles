"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const assert_1 = __importDefault(require("assert"));
const utils_1 = require("../formatting/prettier/utils");
suite('Basic Node Formatting', () => {
    test('it formats echos', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)('{{ $title }}').trim(), "{{ $title }}");
    });
    test('it formats echo variant', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)('{{{ $title }}}').trim(), "{{{ $title }}}");
    });
    test('it formats escaped echos', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)('{!! $title !!}').trim(), "{!! $title !!}");
    });
    test('it formats simple comments', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)('{{-- Comment. --}}').trim(), "{{-- Comment. --}}");
    });
    test('it formats block comments', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`{{--
                Block Comment. --}}`).trim(), `{{--
    Block Comment.
--}}`);
    });
    test('it formats block comments with', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`{{--
 Block Comment.
 with many lines and with <stuff></stuff> --}}`), `{{--
    Block Comment.
    with many lines and with <stuff></stuff>
--}}
`);
    });
    test('block comments inside a div', () => {
        assert_1.default.strictEqual((0, utils_1.formatBladeString)(`<div>
        {{--
 Block Comment.
 with many lines and with <stuff></stuff> --}}
        </div>`).trim(), `<div>
    {{--
        Block Comment.
        with many lines and with <stuff></stuff>
    --}}
</div>`);
    });
});
//# sourceMappingURL=formatter_basic_nodes.test.js.map